# config.py — Конфигурация устройства
#
# Этот файл определяет роль устройства в системе.
# Один и тот же набор .py файлов заливается на оба устройства.
# Отличается ТОЛЬКО этот файл.
#
# MODE:
#   "TX" — блок в машине (радар LD2410B, датчик вибрации, отправка тревог)
#   "RX" — брелок (приём тревог, отправка пинга для запроса статуса)

MODE = "TX"
